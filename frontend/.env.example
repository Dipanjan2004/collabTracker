# ===================================
# API Configuration
# ===================================
# In development, Vite proxies /api → localhost:5000 (see vite.config.ts)
# Change this only if your backend runs on a different host
VITE_API_BASE_URL=/api
